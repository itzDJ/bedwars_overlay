# bedwars_overlay

Hypixel Bedwars Terminal Overlay — macOS / Lunar Client 1.8

Setup:
1. clone repo
2. activate virtual env
3. install requirements
4. create .env in project folder with: HYPIXEL_API_KEY=your-key
5. run the python script and type `/who` in game or `/w !IGN` anywhere on Hypixel
